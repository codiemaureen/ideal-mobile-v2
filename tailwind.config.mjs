/** @type {import('tailwindcss').Config} */
const config = {
  content: [], // v4 generally doesn't need this, keep only if you know why
  theme: { extend: {} },
  plugins: [],
};
export default config;
